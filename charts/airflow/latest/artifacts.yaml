name: airflow
repo: bitnami
url: https://charts.bitnami.com/bitnami
chart: bitnami/airflow
version: 23.0.1

dependencies:
  - postgres
  - redis

resources:
  configmap:
    message: "Airflow dags local path? "
    name: dags.existingConfigmap
    value: "airflow-dags"